# Profile: high-llm
# Enables live steps with the production LLM routing.

live_eval:
  llm_config_file: freud/config/llm-high-llm.yaml
  timeout: 120

pipeline:
  - name: reasoning_eval_model
    live_only: true
