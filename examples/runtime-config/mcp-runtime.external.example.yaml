# Minimal local tooling overlay.
# Decisions made here:
# - keep MCP time enabled through stdio
# - keep native fetch enabled

time:
  enabled: true
  mode: stdio
  provider: mcp-server-time
  command: "uvx mcp-server-time"

fetch:
  enabled: true
  mode: native
  provider: native-jvm
